# Solo nombres de variables — sin credenciales reales en el repositorio.
# Copiar este archivo a .env (en tarea1_rag_normativo/, y luego en
# tarea2_radar/ si aplica) y completar con tus propias claves.

# Tarea 1 — motor RAG normativo (usar la del proveedor activo en config.yaml)
GEMINI_API_KEY=
DEEPSEEK_API_KEY=
OPENAI_API_KEY=
